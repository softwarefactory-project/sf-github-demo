- job:
    name: demo-ansible-lint
    parent: base
    run: tests/ansible-lint.yaml
    nodeset: pod-fedora-30

- project:
    check:
        jobs:
            - demo-ansible-lint
    gate:
        jobs:
            - demo-ansible-lint
